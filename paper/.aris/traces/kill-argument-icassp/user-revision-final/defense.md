# Independent defense and final rebind

Exact final bindings:

- `main.pdf`: `2976344081abbceb84c4114068bd7b37100ae7aa8438d3112b5227bee442731e`
- `sections/3_method.tex`: `c4ab8008065bb8781e402921878f5b99c7fb841acdef24c6129b7d93c988dc84`
- `sections/4_experiments.tex`: `cd6508a60e3e9cac767ccfcec53e019a52525f58cebc2b1559819139abe0ecb5`
- `results_manifest.schema.json`: `9d5c07450cbda32be8223237cb2d764d896d0c9ec28e4c9f23ad7cad3d67e662`

The unsupported `preregistered` claim is resolved: the paper now says the
protocol is prospectively specified but not executed, uses predeclared seeds,
and distinguishes frozen selection rules from predeclared aggregates. The
eligibility loopholes are also resolved. Pending or excluded manifests cannot
set `table_eligible=true`; frozen-eligible requires populated dataset, protocol,
statistics, artifacts, metrics, hash-bound passing audits, paper bindings, all
eligibility flags, and no blockers.

The current empty manifest is therefore an honest ineligible record rather than
a gating defect. Predicted-track identity lifecycle remains unresolved and
still defeats that branch, although it does not independently defeat the
oracle/supplied-track formulation.

Remaining adjudication:

- **P1 survives — fatal:** no empirical outcomes or eligible MultiRep artifact
- **P2 survives — fatal:** no synchronized proposed multi-person implementation
- **P3 survives — fatal reproducibility:** major interfaces, decoder behavior,
  losses, gradients, and lifecycle remain source-unbound
- **P4 survives — high/potentially fatal:** novelty is articulated but not yet
  established through frozen method/evidence
- **P5 survives narrowly — high:** predicted-track lifecycle is unresolved;
  preregistration and schema attacks are defeated
- **P6 survives — major:** declarations, media rights, complete authorship, and
  final 2027-kit binding remain pending

The earlier equation-punctuation `FAIL` label was a non-surviving inversion:
the user explicitly required punctuation-free displays, Eq. (1) has corrected
indexed notation, and the final validator passes all four groups.

`KILL_DEFENSE_VERDICT: KILL_SURVIVES`
