# Claude Reviewer Health

The ARIS Claude-review overlay is installed, but two live probes failed because
the local Claude CLI did not return the JSON payload expected by the MCP bridge.
No cross-family review has been obtained for the ICASSP draft. Same-family
review may continue, but the submission status is capped at `provisional`.
